unit UExtrato_Comissao_RDP;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls, DBTables, DB, IBCustomDataSet, IBQuery, Buttons, ExtCtrls,
  rxCurrEdit, Mask, rxToolEdit, QRCtrls, QuickRpt, RDprint, FireDAC.Stan.Intf,
  FireDAC.Stan.Option, FireDAC.Stan.Param, FireDAC.Stan.Error, FireDAC.DatS,
  FireDAC.Phys.Intf, FireDAC.DApt.Intf, FireDAC.Stan.Async, FireDAC.DApt,
  FireDAC.Comp.Client, FireDAC.Comp.DataSet;

type
  TFrmExtrato_Comissao_RDP = class(TForm)
    Label1: TLabel;
    Label2: TLabel;
    Vendedor: TCurrencyEdit;
    Bevel1: TBevel;
    DataVendedor: TDataSource;
    Label3: TLabel;
    Comissao: TCurrencyEdit;
    Label5: TLabel;
    Entrada: TCurrencyEdit;
    Label4: TLabel;
    Vendas: TComboBox;
    Extrato_Comissao: TQuickRep;
    ColumnHeaderBand1: TQRBand;
    QRSysData1: TQRSysData;
    Cabec: TQRLabel;
    QRSysData2: TQRSysData;
    QRLabel3: TQRLabel;
    QRShape1: TQRShape;
    QRLabel6: TQRLabel;
    QRLabel7: TQRLabel;
    QRLabel5: TQRLabel;
    QRShape2: TQRShape;
    QRLabel10: TQRLabel;
    QRLabel13: TQRLabel;
    QRDBText4: TQRDBText;
    QRLabel4: TQRLabel;
    QRLabel8: TQRLabel;
    QRLabel9: TQRLabel;
    DetailBand1: TQRBand;
    QRDBText1: TQRDBText;
    QRDBText2: TQRDBText;
    QRDBText5: TQRDBText;
    QRDBText6: TQRDBText;
    QRDBText7: TQRDBText;
    QRDBText8: TQRDBText;
    QRGroup1: TQRGroup;
    QRDBText3: TQRDBText;
    QRBand1: TQRBand;
    QRShape3: TQRShape;
    QRShape5: TQRShape;
    QRBand2: TQRBand;
    QRShape4: TQRShape;
    QRShape6: TQRShape;
    QRExpr5: TQRExpr;
    QRExpr1: TQRExpr;
    QRExpr2: TQRExpr;
    QRExpr3: TQRExpr;
    RDprint1: TRDprint;
    Panel1: TPanel;
    CheckBox1: TCheckBox;
    Label6: TLabel;
    Tp_Relatorio: TComboBox;
    btnRetorna: TBitBtn;
    btnExecuta: TBitBtn;
    btnVendedor: TSpeedButton;
    Dtmen: TDateEdit;
    Dtmai: TDateEdit;
    QRel: TFDQuery;
    Qvendedor: TFDQuery;
    Qsearch: TFDQuery;
    QExterno: TFDQuery;
    Relat: TFDMemTable;
    Comissao_Produto: TCheckBox;
    QDevolucao: TFDQuery;
    procedure btnVendedorClick(Sender: TObject);
    procedure VendedorKeyDown(Sender: TObject; var Key: Word;
      Shift: TShiftState);
    procedure DtmenKeyDown(Sender: TObject; var Key: Word; Shift: TShiftState);
    procedure btnExecutaClick(Sender: TObject);
    procedure DtmenEnter(Sender: TObject);
    procedure FormClose(Sender: TObject; var Action: TCloseAction);
    procedure FormCreate(Sender: TObject);
    procedure btnRetornaClick(Sender: TObject);
    procedure RDprint1NewPage(Sender: TObject; Pagina: Integer);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  FrmExtrato_Comissao_RDP: TFrmExtrato_Comissao_RDP;
  Pagina, linha: Integer;
  Nome : String;
  Total_Venda, Total_Comissao : Real ;

implementation

uses
  UPrincipal, UData, UConsulta;

{$R *.dfm}

procedure TFrmExtrato_Comissao_RDP.btnExecutaClick(Sender: TObject);
var
Vl_Venda, Per_Com, Per_Acr,Vl_Devolucao: Double;
Temp_Name: String;
begin
 // 203, 327
  try

    if Comissao.Value = 0 Then
    Begin
      Application.MessageBox('Valor da comissão não pode ser 0,00.', PChar(Msg_Title), mb_IconInformation);
      Comissao.SetFocus;
      Abort;
    End;

    with Relat do
    begin

      Temp_Name    := Copy(TimeToStr(time), 1, 2) + Copy(TimeToStr(time), 4, 2) + Copy(TimeToStr(time), 7, 2);

      with FieldDefs do
      begin
        Clear;
        Add('CODIGO', ftAutoInc, 0, False);
        Add('NOME', ftString, 40, False);
        Add('DATA', ftDate, 0, False);
        Add('NUM_DOC', ftString, 9, False);
        Add('CLIENTE', ftString, 40, False);
        Add('VR_VENDA', ftFloat, 0, False);
        Add('PERCENT', ftFloat, 0, False);
        Add('VR_COMISSAO', ftFloat, 0, False);
        Add('TRANSACAO_ID', ftInteger, 0, False);
      end;

      with IndexDefs do
      begin
        Clear;
        Add('', 'CODIGO', [ixPrimary, ixUnique]);
        Add('TEMP', 'NOME;DATA', [ixCaseInsensitive]);
      end;

      Open;
    end;

   if Vendas.Text <> 'A Liquidar' then
   Begin
      if Comissao_Produto.Checked Then
      Begin
        QRel.Sql.Clear;
        QRel.Sql.Add('SELECT TRANSACOES.TRANSACAO_ID,TRANSACOES.COND_PAGTO,(TRANSITENS.VR_TOTAL + TRANSITENS.VALOR_ICMS_ST + TRANSITENS.VR_FRETE + TRANSITENS.VR_ACRESCIMO + TRANSITENS.VR_IPI - TRANSITENS.DESC_RODAPE) VALOR,' +
                     'TRANSACOES.DT_TRANS,TRANSACOES.NUM_DOC,TRANSACOES.CONDUTA,TRANSACOES.DEPTO,TRANSITENS.COMISSAO COMISSAO_PROD,  FUNCIONARIOS.NOME, CLIENTES.NOME CLIENTE');
        QRel.Sql.Add('FROM TRANSITENS');
        QRel.Sql.Add('INNER JOIN TRANSACOES');
        QRel.Sql.Add('ON (TRANSACOES.TRANSACAO_ID = TRANSITENS.TRANSACAO_ID)');
        QRel.Sql.Add('INNER JOIN FUNCIONARIOS');
        QRel.Sql.Add('ON (FUNCIONARIOS.FUNCIONARIO_ID = TRANSACOES.VENDEDOR_ID)');
        QRel.Sql.Add('LEFT JOIN CLIENTES');
        QRel.Sql.Add('ON (CLIENTES.CLIENTE_ID = TRANSACOES.CLIENTE_ID)');
        QRel.Sql.Add('WHERE');
        QRel.Sql.Add('(TRANSACOES.EMPRESA_ID = :EMPRESA_ID)');
        QRel.Sql.Add('AND ((TRANSACOES.CONDUTA = :CD_VDA AND TRANSACOES.DEPTO = :DP_VDA) OR (TRANSACOES.CONDUTA = :CD_DEV AND TRANSACOES.DEPTO = :DP_DEV))');

        if  not Caso(Copy(FrmData.QAcesso.FieldByName('OPCOES').AsString, 66, 1))  Then
        QRel.Sql.Add('AND (TRANSACOES.TPCTB <= :TPCTB)');

        QRel.Sql.Add('AND (TRANSACOES.TRANSACAO_ID NOT IN (SELECT TRANSACAO_ID FROM NOTAS_CANCELADAS_NFCE))');

        // ACRESCENTADO DIA 02/08/16 - Wanderson
        QRel.Sql.Add('AND TRANSACOES.TRANSACAO_ID NOT IN(SELECT TRANSACAO_ID FROM NOTAS_CANCELADAS)');
        ///////

        QRel.Sql.Add('AND (TRANSACOES.DT_TRANS BETWEEN :DT_INICIAL AND :DT_FINAL)');

        if Vendedor.Value > 0 then
        begin
          QRel.Sql.Add('AND (TRANSACOES.VENDEDOR_ID = :VENDEDOR_ID)');
          QRel.ParamByName('VENDEDOR_ID').AsInteger := StrToInt(Vendedor.Text);
        end
        else
          QRel.Sql.Add('AND (TRANSACOES.VENDEDOR_ID > 0)');

        // QRel.Sql.Add('GROUP BY FUNCIONARIOS.NOME,CLIENTES.NOME,SERVICOS.COMISSAO,TRANSACOES.TRANSACAO_ID,TRANSACOES.COND_PAGTO,TRANSACOES.DT_TRANS');

        QRel.Sql.Add('ORDER BY FUNCIONARIOS.NOME, TRANSACOES.DT_TRANS');

        QRel.ParamByName('EMPRESA_ID').AsInteger  := FrmPrincipal.QEmpresa.FieldByName('EMPRESA_ID').AsInteger;  //FrmData.QAcesso.FieldByName('EMPRESA_ID').AsInteger;
        QRel.ParamByName('CD_VDA').AsString       := '01';
        QRel.ParamByName('DP_VDA').AsString       := '07';
        QRel.ParamByName('CD_DEV').AsString       := '02';
        QRel.ParamByName('DP_DEV').AsString       := '10';

        if not  Caso(Copy(FrmData.QAcesso.FieldByName('OPCOES').AsString, 66, 1))  Then
        QRel.ParamByName('TPCTB').AsString        := FrmData.QAcesso.FieldByName('TPCTB').AsString;

        QRel.ParamByName('DT_INICIAL').AsDateTime := Dtmen.Date;
        QRel.ParamByName('DT_FINAL').AsDateTime   := Dtmai.Date;

        QRel.Prepare;
        QRel.Open;

        QRel.First;
      End
      Else
      Begin

      QRel.Sql.Clear;
      QRel.Sql.Add('SELECT TRANSACOES.*, FUNCIONARIOS.NOME, CLIENTES.NOME CLIENTE,FUNCIONARIOS.COMISSAO COMISSAO_FUN');
      QRel.Sql.Add('FROM TRANSACOES');
      QRel.Sql.Add('INNER JOIN FUNCIONARIOS');
      QRel.Sql.Add('ON (FUNCIONARIOS.FUNCIONARIO_ID = TRANSACOES.VENDEDOR_ID)');
      QRel.Sql.Add('LEFT JOIN CLIENTES');
      QRel.Sql.Add('ON (CLIENTES.CLIENTE_ID = TRANSACOES.CLIENTE_ID)');
      QRel.Sql.Add('WHERE');
      QRel.Sql.Add('(TRANSACOES.EMPRESA_ID = :EMPRESA_ID)');
      QRel.Sql.Add('AND ((TRANSACOES.CONDUTA = :CD_VDA AND TRANSACOES.DEPTO = :DP_VDA) OR (TRANSACOES.CONDUTA = :CD_DEV AND TRANSACOES.DEPTO = :DP_DEV))');
      //INCLUIDO FUNÇÃO MADEREIRA JAMAR

       if not  Caso(Copy(FrmData.QAcesso.FieldByName('OPCOES').AsString, 66, 1)) Then
      QRel.Sql.Add('AND (TRANSACOES.TPCTB <= :TPCTB)');


      // ACRESCENTADO DIA 02/08/16 - Wanderson
      QRel.Sql.Add('AND TRANSACOES.TRANSACAO_ID NOT IN(SELECT TRANSACAO_ID FROM NOTAS_CANCELADAS)');
      QRel.Sql.Add('AND TRANSACOES.TRANSACAO_ID NOT IN(SELECT TRANSACAO_ID FROM NOTAS_CANCELADAS_NFCE)');
      ///////

      QRel.Sql.Add('AND (TRANSACOES.DT_TRANS BETWEEN :DT_INICIAL AND :DT_FINAL)');

      if Vendedor.Value > 0 then
      begin
        QRel.Sql.Add('AND (TRANSACOES.VENDEDOR_ID = :VENDEDOR_ID)');
        QRel.ParamByName('VENDEDOR_ID').AsInteger := StrToInt(Vendedor.Text);
      end
      else
        QRel.Sql.Add('AND (TRANSACOES.VENDEDOR_ID > 0)');

      QRel.Sql.Add('ORDER BY FUNCIONARIOS.NOME, TRANSACOES.DT_TRANS');

      QRel.ParamByName('EMPRESA_ID').AsInteger  := FrmPrincipal.QEmpresa.FieldByName('EMPRESA_ID').AsInteger;  //FrmData.QAcesso.FieldByName('EMPRESA_ID').AsInteger;
      QRel.ParamByName('CD_VDA').AsString       := '01';
      QRel.ParamByName('DP_VDA').AsString       := '07';
      QRel.ParamByName('CD_DEV').AsString       := '02';
      QRel.ParamByName('DP_DEV').AsString       := '10';

      if  not Caso(Copy(FrmData.QAcesso.FieldByName('OPCOES').AsString, 66, 1))  Then
      QRel.ParamByName('TPCTB').AsString        := FrmData.QAcesso.FieldByName('TPCTB').AsString;

      QRel.ParamByName('DT_INICIAL').AsDateTime := Dtmen.Date;
      QRel.ParamByName('DT_FINAL').AsDateTime   := Dtmai.Date;

      QRel.Prepare;
      QRel.Open;

      QRel.First;
      End;


    while not QRel.Eof do
    begin
     { QDevolucao.Sql.Clear;
      QDevolucao.Sql.Add('SELECT VALOR FROM TRANSACOES WHERE SAIDA_ID  = :SAIDA_ID');
      QDevolucao.ParamByName('SAIDA_ID').AsInteger :=  QRel.FieldByName('TRANSACAO_ID').AsInteger;
      QDevolucao.Prepare;
      QDevolucao.Open;

      If not QDevolucao.isEmpty Then
      Vl_Devolucao := QDevolucao.FieldByname('VALOR').AsFloat
      Else
      Vl_Devolucao := 0;
      }

      if Comissao_Produto.Checked Then
      Begin
        if Comissao.Value = 0 then
        Per_Com := QRel.FieldByName('COMISSAO_PROD').AsFloat
        else
        Per_Com := Comissao.Value;

        Vl_Venda := 0;
        Vl_Venda := QRel.FieldByName('VALOR').AsFloat ;

      End
      Else
      Begin

      if Comissao.Value = 0 then
        Per_Com := QRel.FieldByName('COMISSAO_FUN').AsFloat
      else
        Per_Com := Comissao.Value;

      Vl_Venda := 0;

      if QRel.FieldByName('CONDUTA').AsString = '01' then
        Vl_Venda := QRel.FieldByName('VALOR').AsFloat - QRel.FieldByName('VR_ACRESCIMO').AsFloat ;

      if QRel.FieldByName('CONDUTA').AsString = '02' then
        Vl_Venda := (QRel.FieldByName('VALOR').AsFloat * -1);
      End;

      if (QRel.FieldByName('COND_PAGTO').AsString = 'A PRAZO') AND (QRel.FieldByName('CONDUTA').AsString = '01')  then
      begin

        if Vendas.Text = 'Liquidadas' then
        begin
          QExterno.Sql.Clear;
          QExterno.Sql.Add('SELECT * FROM TRANSPARCELAS');
          QExterno.Sql.Add('WHERE');
          QExterno.Sql.Add('(TRANSACAO_ID = :TRANSACAO_ID)');
          QExterno.Sql.Add('AND (BANCO_ID > 0)');
          QExterno.Sql.Add('AND (TIPO_TRANSACAO = :TIPO_TRANSACAO)');

          QExterno.ParamByName('TRANSACAO_ID').AsInteger  := QRel.FieldByName('TRANSACAO_ID').AsInteger;
          QExterno.ParamByName('TIPO_TRANSACAO').AsString := 'T';

          QExterno.Prepare;
          QExterno.Open;

          if QRel.FieldByName('VR_ACRESCIMO').AsFloat > 0 then
            Per_Acr := ((QRel.FieldByName('VR_ACRESCIMO').AsFloat * 100) / QRel.FieldByName('VALOR').AsFloat)
          else
            Per_Acr := 0;

          if Per_Acr > 0 then
            Vl_Venda := (QExterno.FieldByName('VALOR').AsFloat - ((QExterno.FieldByName('VALOR').AsFloat * Per_Acr) / 100))
          else
            Vl_Venda := QExterno.FieldByName('VALOR').AsFloat;

          Per_Com := Entrada.Value;
        end
        else
          Vl_Venda := QRel.FieldByName('VALOR').AsFloat ;
      end;

      if (Vl_Venda * Per_Com) <> 0  then
      begin
        Relat.Append;
        Relat.FieldByName('NOME').AsString       := QRel.FieldByName('NOME').AsString;
        Relat.FieldByName('DATA').AsString       := QRel.FieldByName('DT_TRANS').AsString;
        Relat.FieldByName('NUM_DOC').AsString    := QRel.FieldByName('NUM_DOC').AsString;
        Relat.FieldByName('CLIENTE').AsString    := QRel.FieldByName('CLIENTE').AsString;
        Relat.FieldByName('VR_VENDA').AsFloat    := Vl_Venda;
        Relat.FieldByName('PERCENT').AsFloat     := Per_Com;
        Relat.FieldByName('VR_COMISSAO').AsFloat := ((Relat.FieldByName('VR_VENDA').AsFloat * Relat.FieldByName('PERCENT').AsFloat) / 100);
        Relat.FieldByName('TRANSACAO_ID').AsInteger  := QRel.FieldByName('TRANSACAO_ID').AsInteger;
        Relat.Post;
      end;

      Application.ProcessMessages;
      QRel.Next;
    end;
    QRLabel3.Caption := '*** EXTRATO DE COMISSÃO DE VENDEDOR EFETUADAS ***'
   End;


    if (Vendas.Text = 'Liquidadas')  then
    begin
      QRel.Sql.Clear;
      QRel.Sql.Add('SELECT * FROM TRANSACOES');
      QRel.Sql.Add('WHERE');
      QRel.Sql.Add('(EMPRESA_ID = :EMPRESA_ID)');
      QRel.Sql.Add('AND (CONDUTA = :CONDUTA) AND (PARCELA_ID = ''F'') ');

      if  NOT Caso(Copy(FrmData.QAcesso.FieldByName('OPCOES').AsString, 66, 1)) Then
      QRel.Sql.Add('AND (TPCTB <= :TPCTB)');

      QRel.Sql.Add('AND (DT_TRANS BETWEEN :DT_INICIAL AND :DT_FINAL)');
      QRel.Sql.Add('AND (TRANSACOES.TRANSACAO_ID NOT IN (SELECT TRANSACAO_ID FROM NOTAS_CANCELADAS_NFCE))');
      QRel.Sql.Add('ORDER BY DT_TRANS');

      QRel.ParamByName('EMPRESA_ID').AsInteger  := FrmPrincipal.QEmpresa.FieldByName('EMPRESA_ID').AsInteger;  //FrmData.QAcesso.FieldByName('EMPRESA_ID').AsInteger;
      QRel.ParamByName('CONDUTA').AsString      := '01';

      if NOT  Caso(Copy(FrmData.QAcesso.FieldByName('OPCOES').AsString, 66, 1)) Then
      QRel.ParamByName('TPCTB').AsString        := FrmData.QAcesso.FieldByName('TPCTB').AsString;


      QRel.ParamByName('DT_INICIAL').AsDateTime := Dtmen.Date;
      QRel.ParamByName('DT_FINAL').AsDateTime   := Dtmai.Date;

      QRel.Prepare;
      QRel.Open;

      QRel.First;
      while not QRel.Eof do
      begin
            // SHOWMESSAGE(IntToStr(QRel.FieldByName('TRANSACAO_ID').AsInteger));
          QExterno.Sql.Clear;
          QExterno.Sql.Add('SELECT TRANSACOES.*, FUNCIONARIOS.NOME, CLIENTES.NOME CLIENTE,TRANSPARCELAS.VALOR VALOR_MULTI');
          QExterno.Sql.Add('FROM TRANSPARCELAS');
          QExterno.Sql.Add('INNER JOIN TRANSACOES');
          QExterno.Sql.Add('ON (TRANSACOES.TRANSACAO_ID = TRANSPARCELAS.TRANSACAO_ID)');
          QExterno.Sql.Add('INNER JOIN FUNCIONARIOS');
          QExterno.Sql.Add('ON (FUNCIONARIOS.FUNCIONARIO_ID = TRANSACOES.VENDEDOR_ID)');
          QExterno.Sql.Add('LEFT JOIN CLIENTES');
          QExterno.Sql.Add('ON (CLIENTES.CLIENTE_ID = TRANSACOES.CLIENTE_ID)');
          QExterno.Sql.Add('WHERE');
          QExterno.Sql.Add('(TRANSPARCELAS.TRANSACAO_ID = :TRANSACAO_ID)');
          QExterno.Sql.Add('AND (TRANSPARCELAS.TIPO_TRANSACAO = :TIPO_TRANSACAO)');
          QExterno.Sql.Add('AND (TRANSACOES.EMPRESA_ID = :EMPRESA_ID) AND (TRANSPARCELAS.BANCO_ID > 0)');

          QExterno.ParamByName('TRANSACAO_ID').AsInteger   := QRel.FieldByName('TRANSACAO_ID').AsInteger;
          QExterno.ParamByName('TIPO_TRANSACAO').AsString  := 'T';
          QExterno.ParamByName('EMPRESA_ID').AsInteger     := FrmPrincipal.QEmpresa.FieldByName('EMPRESA_ID').AsInteger;  //FrmData.QAcesso.FieldByName('EMPRESA_ID').AsInteger;

          if Vendedor.Value > 0 then
          begin
            QExterno.Sql.Add('AND (TRANSACOES.VENDEDOR_ID = :VENDEDOR_ID)');

            QExterno.ParamByName('VENDEDOR_ID').AsInteger := StrToInt(Vendedor.Text);
          end
          else
            QExterno.Sql.Add('AND (TRANSACOES.VENDEDOR_ID > 0)');

          QExterno.Prepare;
          QExterno.Open;
          QExterno.First;

          while not QExterno.Eof do
          Begin

            //SHOWMESSAGE(QExterno.FieldByName('NOME').AsString);

            if QExterno.FieldByName('NOME').AsString <> '' then
            begin
              if QExterno.FieldByName('VR_ACRESCIMO').AsFloat > 0 then
                Per_Acr := ((QExterno.FieldByName('VR_ACRESCIMO').AsFloat * 100) / QExterno.FieldByName('VALOR').AsFloat)
              else
                Per_Acr := 0;

              if Per_Acr > 0 then
                Vl_Venda := (QExterno.FieldByName('VALOR_MULTI').AsFloat - ((QExterno.FieldByName('VALOR_MULTI').AsFloat * Per_Acr) / 100))
              else
                Vl_Venda := QExterno.FieldByName('VALOR_MULTI').AsFloat;

              if Comissao.Value = 0 then
                Per_Com := QExterno.FieldByName('COMISSAO').AsFloat
              else
                Per_Com := Comissao.Value;

              if (Vl_Venda * Per_Com) <> 0 then
              begin
                Relat.Append;
                Relat.FieldByName('NOME').AsString       := QExterno.FieldByName('NOME').AsString;
                Relat.FieldByName('DATA').AsString       := QExterno.FieldByName('DT_TRANS').AsString;
                Relat.FieldByName('NUM_DOC').AsString    := QExterno.FieldByName('NUM_DOC').AsString;
                Relat.FieldByName('CLIENTE').AsString    := QExterno.FieldByName('CLIENTE').AsString;
                Relat.FieldByName('VR_VENDA').AsFloat    := Vl_Venda ;
                Relat.FieldByName('PERCENT').AsFloat     := Per_Com;
                Relat.FieldByName('VR_COMISSAO').AsFloat := ((Relat.FieldByName('VR_VENDA').AsFloat * Relat.FieldByName('PERCENT').AsFloat) / 100);
                Relat.Post;
              end;
            end;
            QExterno.Next;
          End;
       QRel.Next;
      end;

      QRel.Sql.Clear;
      QRel.Sql.Add('SELECT * FROM TRANSACOES');
      QRel.Sql.Add('WHERE');
      QRel.Sql.Add('(EMPRESA_ID = :EMPRESA_ID)');
      QRel.Sql.Add('AND (CONDUTA = :CONDUTA)');

      if  NOT Caso(Copy(FrmData.QAcesso.FieldByName('OPCOES').AsString, 66, 1)) Then
      QRel.Sql.Add('AND (TPCTB <= :TPCTB)');

      QRel.Sql.Add('AND (DT_TRANS BETWEEN :DT_INICIAL AND :DT_FINAL)');
      QRel.Sql.Add('AND (TRANSACOES.TRANSACAO_ID NOT IN (SELECT TRANSACAO_ID FROM NOTAS_CANCELADAS_NFCE))');
      QRel.Sql.Add('ORDER BY DT_TRANS');

      QRel.ParamByName('EMPRESA_ID').AsInteger  := FrmPrincipal.QEmpresa.FieldByName('EMPRESA_ID').AsInteger;  //FrmData.QAcesso.FieldByName('EMPRESA_ID').AsInteger;
      QRel.ParamByName('CONDUTA').AsString      := '07';

      if NOT  Caso(Copy(FrmData.QAcesso.FieldByName('OPCOES').AsString, 66, 1)) Then
      QRel.ParamByName('TPCTB').AsString        := FrmData.QAcesso.FieldByName('TPCTB').AsString;


      QRel.ParamByName('DT_INICIAL').AsDateTime := Dtmen.Date;
      QRel.ParamByName('DT_FINAL').AsDateTime   := Dtmai.Date;

      QRel.Prepare;
      QRel.Open;

      QRel.First;
      while not QRel.Eof do
      begin
        IF QRel.FieldByName('DEPTO').AsString  = '03' Then
        Begin
          QExterno.Sql.Clear;
          QExterno.Sql.Add('SELECT TRANSACOES.*, FUNCIONARIOS.NOME, CLIENTES.NOME CLIENTE');
          QExterno.Sql.Add('FROM TRANSPARCELAS');
          QExterno.Sql.Add('INNER JOIN TRANSACOES');
          QExterno.Sql.Add('ON (TRANSACOES.TRANSACAO_ID = TRANSPARCELAS.TRANSACAO_ID)');
          QExterno.Sql.Add('INNER JOIN FUNCIONARIOS');
          QExterno.Sql.Add('ON (FUNCIONARIOS.FUNCIONARIO_ID = TRANSACOES.VENDEDOR_ID)');
          QExterno.Sql.Add('INNER JOIN CLIENTES');
          QExterno.Sql.Add('ON (CLIENTES.CLIENTE_ID = TRANSACOES.CLIENTE_ID)');
          QExterno.Sql.Add('WHERE');
          QExterno.Sql.Add('(TRANSPARCELAS.AGRUPAMENTO_ID = :AGRUPAMENTO_ID)');
          QExterno.Sql.Add('AND (TRANSPARCELAS.TIPO_TRANSACAO = :TIPO_TRANSACAO)');
          QExterno.Sql.Add('AND (TRANSACOES.EMPRESA_ID = :EMPRESA_ID)');

          QExterno.ParamByName('AGRUPAMENTO_ID').AsInteger := QRel.FieldByName('TRANSACAO_ID').AsInteger;
          QExterno.ParamByName('TIPO_TRANSACAO').AsString  := 'T';
          QExterno.ParamByName('EMPRESA_ID').AsInteger     := FrmPrincipal.QEmpresa.FieldByName('EMPRESA_ID').AsInteger;  //FrmData.QAcesso.FieldByName('EMPRESA_ID').AsInteger;

          if Vendedor.Value > 0 then
          begin
            QExterno.Sql.Add('AND (TRANSACOES.VENDEDOR_ID = :VENDEDOR_ID)');

            QExterno.ParamByName('VENDEDOR_ID').AsInteger := StrToInt(Vendedor.Text);
          end
          else
            QExterno.Sql.Add('AND (TRANSACOES.VENDEDOR_ID > 0)');

          QExterno.Prepare;
          QExterno.Open;
          QExterno.First;

          while not QExterno.Eof do
          Begin

            if QExterno.FieldByName('NOME').AsString <> '' then
            begin
              if QExterno.FieldByName('VR_ACRESCIMO').AsFloat > 0 then
                Per_Acr := ((QExterno.FieldByName('VR_ACRESCIMO').AsFloat * 100) / QExterno.FieldByName('VALOR').AsFloat)
              else
                Per_Acr := 0;

              if Per_Acr > 0 then
                Vl_Venda := (QExterno.FieldByName('VALOR').AsFloat - ((QExterno.FieldByName('VALOR').AsFloat * Per_Acr) / 100))
              else
                Vl_Venda := QExterno.FieldByName('VALOR').AsFloat;

              if Comissao.Value = 0 then
                Per_Com := QExterno.FieldByName('COMISSAO').AsFloat
              else
                Per_Com := Comissao.Value;

              if (Vl_Venda * Per_Com) <> 0 then
              begin
                Relat.Append;
                Relat.FieldByName('NOME').AsString       := QExterno.FieldByName('NOME').AsString;
                Relat.FieldByName('DATA').AsString       := QExterno.FieldByName('DT_TRANS').AsString;
                Relat.FieldByName('NUM_DOC').AsString    := QExterno.FieldByName('NUM_DOC').AsString;
                Relat.FieldByName('CLIENTE').AsString    := QExterno.FieldByName('CLIENTE').AsString;
                Relat.FieldByName('VR_VENDA').AsFloat    := Vl_Venda ;
                Relat.FieldByName('PERCENT').AsFloat     := Per_Com;
                Relat.FieldByName('VR_COMISSAO').AsFloat := ((Relat.FieldByName('VR_VENDA').AsFloat * Relat.FieldByName('PERCENT').AsFloat) / 100);
                Relat.Post;
              end;
            end;
            QExterno.Next;
          End;

        End
        Else
        Begin

          QExterno.Sql.Clear;
          QExterno.Sql.Add('SELECT TRANSACOES.*, FUNCIONARIOS.NOME, CLIENTES.NOME CLIENTE');
          QExterno.Sql.Add('FROM TRANSPARCELAS');
          QExterno.Sql.Add('INNER JOIN TRANSACOES');
          QExterno.Sql.Add('ON (TRANSACOES.TRANSACAO_ID = TRANSPARCELAS.TRANSACAO_ID)');
          QExterno.Sql.Add('INNER JOIN FUNCIONARIOS');
          QExterno.Sql.Add('ON (FUNCIONARIOS.FUNCIONARIO_ID = TRANSACOES.VENDEDOR_ID)');
          QExterno.Sql.Add('INNER JOIN CLIENTES');
          QExterno.Sql.Add('ON (CLIENTES.CLIENTE_ID = TRANSACOES.CLIENTE_ID)');
          QExterno.Sql.Add('WHERE');
          QExterno.Sql.Add('(TRANSPARCELAS.TRANSACAO_ID = :SAIDA_ID)');
          QExterno.Sql.Add('AND (TRANSPARCELAS.TIPO_TRANSACAO = :TIPO_TRANSACAO)');
          QExterno.Sql.Add('AND (TRANSACOES.EMPRESA_ID = :EMPRESA_ID)');

          QExterno.ParamByName('SAIDA_ID').AsInteger      := QRel.FieldByName('SAIDA_ID').AsInteger;
          QExterno.ParamByName('TIPO_TRANSACAO').AsString := 'T';
          QExterno.ParamByName('EMPRESA_ID').AsInteger    := FrmPrincipal.QEmpresa.FieldByName('EMPRESA_ID').AsInteger;  //FrmData.QAcesso.FieldByName('EMPRESA_ID').AsInteger;

          if Vendedor.Value > 0 then
          begin
            QExterno.Sql.Add('AND (TRANSACOES.VENDEDOR_ID = :VENDEDOR_ID)');

            QExterno.ParamByName('VENDEDOR_ID').AsInteger := StrToInt(Vendedor.Text);
          end
          else
            QExterno.Sql.Add('AND (TRANSACOES.VENDEDOR_ID > 0)');

          QExterno.Open;
          if QExterno.FieldByName('NOME').AsString <> '' then
          begin
            if QExterno.FieldByName('VR_ACRESCIMO').AsFloat > 0 then
              Per_Acr := ((QExterno.FieldByName('VR_ACRESCIMO').AsFloat * 100) / QExterno.FieldByName('VALOR').AsFloat)
            else
              Per_Acr := 0;

            if Per_Acr > 0 then
              Vl_Venda := (QRel.FieldByName('VALOR').AsFloat - ((QRel.FieldByName('VALOR').AsFloat * Per_Acr) / 100))
            else
              Vl_Venda := QRel.FieldByName('VALOR').AsFloat;

            if Comissao.Value = 0 then
              Per_Com := QExterno.FieldByName('COMISSAO').AsFloat
            else
              Per_Com := Comissao.Value;

            if (Vl_Venda * Per_Com) <> 0 then
            begin
              Relat.Append;
              Relat.FieldByName('NOME').AsString       := QExterno.FieldByName('NOME').AsString;
              Relat.FieldByName('DATA').AsString       := QRel.FieldByName('DT_TRANS').AsString;
              Relat.FieldByName('NUM_DOC').AsString    := QRel.FieldByName('NUM_DOC').AsString;
              Relat.FieldByName('CLIENTE').AsString    := QExterno.FieldByName('CLIENTE').AsString;
              Relat.FieldByName('VR_VENDA').AsFloat    := Vl_Venda ;
              Relat.FieldByName('PERCENT').AsFloat     := Per_Com;
              Relat.FieldByName('VR_COMISSAO').AsFloat := ((Relat.FieldByName('VR_VENDA').AsFloat * Relat.FieldByName('PERCENT').AsFloat) / 100);
              Relat.Post;
            end;
          end;
        End;

        Application.ProcessMessages;
        QRel.Next;
      end;
      QRLabel3.Caption := '*** EXTRATO DE COMISSÃO DE VENDEDOR LIQUIDADAS ***'
    end;

    if (Vendas.Text = 'A Liquidar')  then
    begin
      QRel.Sql.Clear;
      QRel.Sql.Add('SELECT * FROM TRANSACOES');
      QRel.Sql.Add('WHERE');
      QRel.Sql.Add('(EMPRESA_ID = :EMPRESA_ID)');
      QRel.Sql.Add('AND (CONDUTA = :CONDUTA) AND (DEPTO = :DEPTO)');
      QRel.Sql.Add('AND (COND_PAGTO = :COND_PAGTO)');

       if not  Caso(Copy(FrmData.QAcesso.FieldByName('OPCOES').AsString, 66, 1))  Then
       QRel.Sql.Add('AND (TPCTB <= :TPCTB)');

      QRel.Sql.Add('AND (DT_ENT_SAI BETWEEN :DT_INICIAL AND :DT_FINAL)');
      QRel.Sql.Add('AND (TRANSACOES.TRANSACAO_ID NOT IN (SELECT TRANSACAO_ID FROM NOTAS_CANCELADAS_NFCE))');
      QRel.Sql.Add('ORDER BY DT_ENT_SAI');

      QRel.ParamByName('EMPRESA_ID').AsInteger  := FrmPrincipal.QEmpresa.FieldByName('EMPRESA_ID').AsInteger;  //FrmData.QAcesso.FieldByName('EMPRESA_ID').AsInteger;
      QRel.ParamByName('CONDUTA').AsString      := '01';
      QRel.ParamByName('DEPTO').AsString        := '07';
      QRel.ParamByName('COND_PAGTO').AsString   := 'A PRAZO';

       if not  Caso(Copy(FrmData.QAcesso.FieldByName('OPCOES').AsString, 66, 1)) Then
        QRel.ParamByName('TPCTB').AsString        := FrmData.QAcesso.FieldByName('TPCTB').AsString;

      QRel.ParamByName('DT_INICIAL').AsDateTime := Dtmen.Date;
      QRel.ParamByName('DT_FINAL').AsDateTime   := Dtmai.Date;

      QRel.Prepare;
      QRel.Open;

      QRel.First;
      while not QRel.Eof do
      begin
        QExterno.Sql.Clear;
        QExterno.Sql.Add('SELECT TRANSACOES.*, TRANSPARCELAS.VALOR VALOR_REC, FUNCIONARIOS.NOME, CLIENTES.NOME CLIENTE,FUNCIONARIOS.COMISSAO COMISSAO_FUN');
        QExterno.Sql.Add('FROM TRANSPARCELAS');
        QExterno.Sql.Add('INNER JOIN TRANSACOES');
        QExterno.Sql.Add('ON (TRANSACOES.TRANSACAO_ID = TRANSPARCELAS.TRANSACAO_ID)');
        QExterno.Sql.Add('INNER JOIN FUNCIONARIOS');
        QExterno.Sql.Add('ON (FUNCIONARIOS.FUNCIONARIO_ID = TRANSACOES.VENDEDOR_ID)');
        QExterno.Sql.Add('INNER JOIN CLIENTES');
        QExterno.Sql.Add('ON (CLIENTES.CLIENTE_ID = TRANSACOES.CLIENTE_ID)');
        QExterno.Sql.Add('WHERE');
        QExterno.Sql.Add('(TRANSPARCELAS.TRANSACAO_ID = :TRANSACAO_ID)');
        QExterno.Sql.Add('AND (TRANSPARCELAS.BANCO_ID = 0)');
        QExterno.Sql.Add('AND (TRANSPARCELAS.VALOR  > 0)');
        QExterno.Sql.Add('AND (TRANSPARCELAS.TIPO_TRANSACAO = :TIPO_TRANSACAO)');
        QExterno.Sql.Add('AND (TRANSACOES.EMPRESA_ID = :EMPRESA_ID)');

        QExterno.ParamByName('TRANSACAO_ID').AsInteger  := QRel.FieldByName('TRANSACAO_ID').AsInteger;
        QExterno.ParamByName('TIPO_TRANSACAO').AsString := 'T';
        QExterno.ParamByName('EMPRESA_ID').AsInteger    := FrmPrincipal.QEmpresa.FieldByName('EMPRESA_ID').AsInteger;   //FrmData.QAcesso.FieldByName('EMPRESA_ID').AsInteger;

        if Vendedor.Value > 0 then
        begin
          QExterno.Sql.Add('AND (TRANSACOES.VENDEDOR_ID = :VENDEDOR_ID)');

          QExterno.ParamByName('VENDEDOR_ID').AsInteger := StrToInt(Vendedor.Text);
        end
        else
          QExterno.Sql.Add('AND (TRANSACOES.VENDEDOR_ID > 0)');

        QExterno.Open;


       while not QExterno.Eof do
       Begin

        if QExterno.FieldByName('NOME').AsString <> '' then
        begin
          {if QExterno.FieldByName('VR_ACRESCIMO').AsFloat > 0 then
            Per_Acr := ((QExterno.FieldByName('VR_ACRESCIMO').AsFloat * 100) / QExterno.FieldByName('VALOR').AsFloat)
          else
            Per_Acr := 0;

          if Per_Acr > 0 then
            Vl_Venda := (QExterno.FieldByName('VALOR_REC').AsFloat - ((QRel.FieldByName('VALOR').AsFloat * Per_Acr) / 100))
          else
            Vl_Venda := QExterno.FieldByName('VALOR_REC').AsFloat;}

          if Comissao.Value = 0 then
            Per_Com := QExterno.FieldByName('COMISSAO_FUN').AsFloat
          else
            Per_Com := Comissao.Value;

         Vl_Venda := QExterno.FieldByName('VALOR_REC').AsFloat;

          if (Vl_Venda * Per_Com) <> 0 then
          begin
            Relat.Append;
            Relat.FieldByName('NOME').AsString       := QExterno.FieldByName('NOME').AsString;
            Relat.FieldByName('DATA').AsString       := QRel.FieldByName('DT_ENT_SAI').AsString;
            Relat.FieldByName('NUM_DOC').AsString    := QRel.FieldByName('NUM_DOC').AsString;
            Relat.FieldByName('CLIENTE').AsString    := QExterno.FieldByName('CLIENTE').AsString;
            Relat.FieldByName('VR_VENDA').AsFloat    := Vl_Venda ;
            Relat.FieldByName('PERCENT').AsFloat     := Per_Com;
            Relat.FieldByName('VR_COMISSAO').AsFloat := ((Relat.FieldByName('VR_VENDA').AsFloat * Relat.FieldByName('PERCENT').AsFloat) / 100);
            Relat.Post;
          end;
        end;
       QExterno.Next;
       End;

        Application.ProcessMessages;
        QRel.Next;
      end;
      QRLabel3.Caption := '*** EXTRATO DE COMISSÃO DE VENDEDOR A LIQUIDAR ***'
    end;

    QRLabel13.Caption := 'Período: ' + Dtmen.Text + ' a ' + Dtmai.Text;

    if Relat.IsEmpty then
      Application.MessageBox('Não há dados para serem impressos', PChar(Msg_Title), mb_IconInformation)
    else
    Begin

     if Tp_Relatorio.text = 'Sintético' then
     Extrato_Comissao.PreviewModal
     Else
     Begin

      Relat.First;

      if CheckBox1.Checked = True then
      Begin
       RDprint1.Impressora := HP;
       RDprint1.FonteTamanhoPadrao := S10cpp;
      End;

      RDprint1.TamanhoQteColunas :=130;
      RDprint1.OpcoesPreview.Preview     := True;
      RDprint1.MostrarProgresso          := True;
      RDprint1.Acentuacao                := SemAcento;
      RDprint1.TamanhoQteLinhas          := 1;
      RDprint1.Abrir;

       if linha > 66  then
        Begin
         RDprint1.Novapagina;
         Pagina := Pagina + 1;
        End;

      Pagina  := 1;
      Nome:='';
      Total_Venda := 0;
      Total_Comissao := 0;


      while not Relat.Eof do
      Begin

        if Nome <> Relat.FieldByName('NOME').AsString then
        Begin
        RDprint1.ImpF(linha, 01, Relat.FieldByName('NOME').AsString,[negrito]);
        Inc(linha);
        Inc(linha);
        Nome:= Relat.FieldByName('NOME').AsString
        End;

          RDprint1.Imp(linha, 12, Relat.FieldByName('DATA').AsString);
          RDprint1.Imp(linha, 25, Relat.FieldByName('NUM_DOC').AsString);
          RDprint1.ImpVal(linha,38,',###,##0.00',Relat.FieldByName('VR_VENDA').AsFloat,[]);
          RDprint1.ImpVal(linha,54,',###,##0.00',Relat.FieldByName('PERCENT').AsFloat,[]);
          RDprint1.ImpVal(linha,71,',###,##0.00',Relat.FieldByName('VR_COMISSAO').AsFloat,[]);
          RDprint1.ImpF(linha,85, Relat.FieldByName('CLIENTE').AsString,[]);

        // implementação 03/12/2015 - Wanderson
          Inc(linha);

         // RDprint1.Imp(linha, 25, 'PRODUTO');
         // RDprint1.Imp(linha, 68, 'QUANTIDADE');
         // RDprint1.Imp(linha, 82, 'VALOR UNITÁRIO');
         // Inc(linha);


          Qsearch.Close;
          Qsearch.SQL.Clear;
          Qsearch.SQL.Add('SELECT * FROM TRANSITENS WHERE TRANSACAO_ID=:TRANSACAO_ID');
          Qsearch.ParamByName('TRANSACAO_ID').AsInteger := Relat.FieldByName('TRANSACAO_ID').AsInteger;
          Qsearch.Prepare;
          Qsearch.Open;

          if not Qsearch.IsEmpty then
          begin
            Qsearch.First;
            while not Qsearch.Eof do
            begin

               RDprint1.Imp(linha, 25, Copy(Qsearch.FieldByName('DESCRICAO').AsString, 1,40));
               //RDprint1.ImpVal(linha, 68,',###,##0.00',Qsearch.FieldByName('QUANTIDADE').AsFloat, []);
               //RDprint1.ImpVal(linha, 82,',###,##0.00',Qsearch.FieldByName('VR_UNITARIO').AsFloat, []);
               Inc(linha);

               Qsearch.Next;
            end;

          end;

          Inc(linha);

          // final ///////////////////////////////////////////////////////////////////




          Total_Venda := Total_Venda +  Relat.FieldByName('VR_VENDA').AsFloat;
          Total_Comissao := Total_Comissao + Relat.FieldByName('VR_COMISSAO').AsFloat;


        if linha > 64  then
        Begin
         RDprint1.Novapagina;
         Pagina := Pagina + 1;
        End;



        Inc(linha);
        Relat.Next;
      End;


      RDprint1.LinhaH(linha,38,48,0,false);
      RDprint1.LinhaH(linha,71,81,0,false);
      Inc(linha);
      RDprint1.ImpVal(linha,38,',###,##0.00', Total_Venda, [negrito]);
      RDprint1.ImpVal(linha,71,',###,##0.00', Total_Comissao, [negrito]);




      Application.ProcessMessages;
      RDprint1.TamanhoQteLinhas:= 66;
      RDprint1.Fechar;
     End;
    End;

  finally
    Relat.IndexName := '';
    Relat.Close;
    //Relat.DeleteTable;
  end;
end;

procedure TFrmExtrato_Comissao_RDP.RDprint1NewPage(Sender: TObject;
  Pagina: Integer);
begin
      RDprint1.Imp(01, 01, FrmPrincipal.QEmpresa.FieldByName('RAZAO').AsString);
      RDprint1.ImpDir(01, (length(FrmPrincipal.QEmpresa.FieldByName('RAZAO').AsString)), 130, 'Sistema de Automação de empresas Eficaz', []);
      RDprint1.Imp(02, 01, 'Data/Hora: ' + DateToStr(date) + ' - ' + TimeToStr(now));
      RDprint1.impDir(02, 80, 130, 'Página: ' + IntToStr(Pagina), []);

      if Vendas.Text = 'Efetuadas' then
         RDprint1.impC(03, 67, '*** EXTRATO DE COMISSÃO DE VENDEDOR - EFETUADAS ***',[negrito]);

      if Vendas.Text = 'Liquidadas' then
         RDprint1.impC(03, 67, '*** EXTRATO DE COMISSÃO DE VENDEDOR - LIQUIDADAS ***',[negrito]);

      if Vendas.Text = 'A Liquidar' then
         RDprint1.impC(03, 67, '*** EXTRATO DE COMISSÃO DE VENDEDOR - A LIQUIDAR ***',[negrito]);

      RDprint1.imp(04, 01, 'Período:'+ datetoStr(dtmen.date) + ' à ' + datetoStr(dtmai.date));
      RDprint1.LinhaH(05,01,130,0,false);
      RDprint1.imp(06, 01, 'Vendedor');
      RDprint1.imp(06, 12, 'Data');
      RDprint1.imp(06, 25, 'Nº Doc.');
      RDprint1.imp(06, 35, 'Valor da Venda');
      RDprint1.imp(06, 55, 'Percentual');
      RDprint1.imp(06, 70, 'Vr. Comissão');
      RDprint1.imp(06, 85, 'Cliente');
      RDprint1.LinhaH(07,01,130,0,false);
      linha:=08;
end;

procedure TFrmExtrato_Comissao_RDP.btnRetornaClick(Sender: TObject);
begin
  Close;
end;

procedure TFrmExtrato_Comissao_RDP.btnVendedorClick(Sender: TObject);
begin
  try
    Vendedor.Value := GetConsulta('PESSOAL', 0, 0, StrToInt(Vendedor.Text));
  except
    Vendedor.Value := GetConsulta('PESSOAL', 0, 0, 0);
  end;
end;

procedure TFrmExtrato_Comissao_RDP.DtmenEnter(Sender: TObject);
begin
  Keybd_Event(VK_LEFT, 0, 0, 0);
end;

procedure TFrmExtrato_Comissao_RDP.DtmenKeyDown(Sender: TObject; var Key: Word;
  Shift: TShiftState);
begin
  if (Key = Vk_Return) or (Key = Vk_Down) then
    Perform(Wm_NextDlgctl, 0, 0);

  if Key = Vk_Up then
    Perform(Wm_NextDlgctl, 1, 0);
end;

procedure TFrmExtrato_Comissao_RDP.FormClose(Sender: TObject;
  var Action: TCloseAction);
begin
  Action := caFree;
end;

procedure TFrmExtrato_Comissao_RDP.FormCreate(Sender: TObject);
begin
  Dtmen.Date := StrToDate('01/' + Copy(DateToStr(date), 4, 7));
  Dtmai.Date := Ult_Dia_Mes(date);

  if   Caso(Copy(FrmData.QAcesso.FieldByName('OPCOES').AsString, 66, 1))  = True Then
  Begin
   QRel.Sql.Clear;
   QRel.Sql.Text := 'SELECT NOME FROM FUNCIONARIOS WHERE FUNCIONARIO_ID = :FUNCIONARIO_ID';
   QRel.ParamByName('FUNCIONARIO_ID').AsInteger :=  FrmData.QAcesso.FieldByName('FUNCIONARIO_ID').AsInteger;
   QRel.Prepare;
   QRel.Open;


   Vendedor.visible := False;
   Vendedor.Text    := IntToStr(FrmData.QAcesso.FieldByName('FUNCIONARIO_ID').AsInteger);
   Label2.Font.Style:= [fsbold];
   Label2.Caption   :='Vendedor:      ' + QRel.FieldByName('NOME').AsString;
   BtnVendedor.Visible      := False;
   Comissao_Produto.Visible := False;
   Vendas.Enabled   := False;
   Vendas.ItemIndex := 1;



  End;
end;

procedure TFrmExtrato_Comissao_RDP.VendedorKeyDown(Sender: TObject; var Key: Word;
  Shift: TShiftState);
begin
  if (Key = Vk_F7) and (Sender = Vendedor) then
    btnVendedorClick(Self);

  if Key = Vk_Return then
    Perform(Wm_NextDlgctl, 0, 0);
end;

end.
