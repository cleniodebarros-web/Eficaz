unit UFim_Vendas_beer;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls, ExtCtrls, Mask, rxToolEdit, DB, IBCustomDataSet, IBQuery,
  QuickRpt, QRCtrls, Buttons, FireDAC.Stan.Intf, FireDAC.Stan.Option,
  FireDAC.Stan.Param, FireDAC.Stan.Error, FireDAC.DatS, FireDAC.Phys.Intf,
  FireDAC.DApt.Intf, FireDAC.Stan.Async, FireDAC.DApt, FireDAC.Comp.DataSet,
  FireDAC.Comp.Client, QRExport, frxClass, frxExportBaseDialog,
  frxExportXLS;

type
  TFrmFim_Vendas_Beer = class(TForm)
    Label1: TLabel;
    Dtmen: TDateEdit;
    Dtmai: TDateEdit;
    Bevel1: TBevel;
    Label2: TLabel;
    Ordem: TComboBox;
    Fim: TQuickRep;
    ColumnHeaderBand1: TQRBand;
    QRSysData1: TQRSysData;
    Cabec: TQRLabel;
    QRSysData2: TQRSysData;
    QRLabel3: TQRLabel;
    QRShape1: TQRShape;
    QRLabel7: TQRLabel;
    QRLabel5: TQRLabel;
    QRShape2: TQRShape;
    QRLabel10: TQRLabel;
    QRLabel13: TQRLabel;
    QRLabel11: TQRLabel;
    QRDBText8: TQRDBText;
    QRLabel4: TQRLabel;
    QRLabel8: TQRLabel;
    QRLabel9: TQRLabel;
    DetailBand1: TQRBand;
    QRDBText1: TQRDBText;
    QRDBText2: TQRDBText;
    QRDBText4: TQRDBText;
    QRDBText5: TQRDBText;
    QRDBText6: TQRDBText;
    QRBand1: TQRBand;
    QRShape4: TQRShape;
    QRLabel20: TQRLabel;
    QRExpr1: TQRExpr;
    QRExpr2: TQRExpr;
    QRExpr3: TQRExpr;
    QRGroup1: TQRGroup;
    QRBand2: TQRBand;
    QRShape3: TQRShape;
    QRLabel2: TQRLabel;
    QRExpr4: TQRExpr;
    QRExpr5: TQRExpr;
    QRExpr6: TQRExpr;
    QRExpr7: TQRExpr;
    QRExpr8: TQRExpr;
    QRDBText3: TQRDBText;
    btnRetorna: TBitBtn;
    btnExecuta: TBitBtn;
    Label3: TLabel;
    Preco: TComboBox;
    Tipo_Venda: TComboBox;
    Label4: TLabel;
    Label5: TLabel;
    Tipo_Rel: TComboBox;
    Sintetico: TQuickRep;
    QRBand3: TQRBand;
    QRSysData3: TQRSysData;
    QRLabel1: TQRLabel;
    QRSysData4: TQRSysData;
    QRLabel6: TQRLabel;
    QRShape5: TQRShape;
    QRLabel14: TQRLabel;
    QRShape6: TQRShape;
    QRLabel16: TQRLabel;
    QRLabel17: TQRLabel;
    QRDBText7: TQRDBText;
    QRLabel18: TQRLabel;
    QRLabel19: TQRLabel;
    QRLabel21: TQRLabel;
    QRBand4: TQRBand;
    QRDBText11: TQRDBText;
    QRDBText12: TQRDBText;
    QRDBText13: TQRDBText;
    QRExpr9: TQRExpr;
    QRExpr10: TQRExpr;
    QRBand5: TQRBand;
    QRShape7: TQRShape;
    QRLabel22: TQRLabel;
    QRExpr11: TQRExpr;
    QRExpr12: TQRExpr;
    QRExpr13: TQRExpr;
    QRLabel12: TQRLabel;
    QRDBText9: TQRDBText;
    QRExpr14: TQRExpr;
    QRExpr16: TQRExpr;
    QRLabel23: TQRLabel;
    QRLabel25: TQRLabel;
    Qrel: TFDQuery;
    ExportarExcel: TQuickRep;
    QRBand6: TQRBand;
    QRSysData5: TQRSysData;
    QRLabel15: TQRLabel;
    QRSysData6: TQRSysData;
    QRLabel24: TQRLabel;
    QRShape8: TQRShape;
    QRLabel26: TQRLabel;
    QRLabel27: TQRLabel;
    QRShape9: TQRShape;
    QRLabel28: TQRLabel;
    QRLabel29: TQRLabel;
    QRLabel30: TQRLabel;
    QRDBText10: TQRDBText;
    QRLabel31: TQRLabel;
    QRLabel32: TQRLabel;
    QRLabel33: TQRLabel;
    QRBand7: TQRBand;
    QRDBText14: TQRDBText;
    QRDBText15: TQRDBText;
    QRDBText16: TQRDBText;
    QRDBText17: TQRDBText;
    QRDBText18: TQRDBText;
    QRExpr15: TQRExpr;
    QRExpr17: TQRExpr;
    QRBand9: TQRBand;
    QRShape11: TQRShape;
    QRLabel36: TQRLabel;
    QRExpr22: TQRExpr;
    QRExpr23: TQRExpr;
    QRExpr24: TQRExpr;
    QRLabel37: TQRLabel;
    frxXLSExport1: TfrxXLSExport;
    DeliveryBalcao: TCheckBox;
    procedure FormCreate(Sender: TObject);
    procedure DtmenKeyDown(Sender: TObject; var Key: Word; Shift: TShiftState);
    procedure OrdemKeyDown(Sender: TObject; var Key: Word; Shift: TShiftState);
    procedure FormClose(Sender: TObject; var Action: TCloseAction);
    procedure btnRetornaClick(Sender: TObject);
    procedure btnExecutaClick(Sender: TObject);
    procedure DtmenEnter(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  FrmFim_Vendas_Beer: TFrmFim_Vendas_Beer;

implementation

uses
  UData, UPrincipal;

{$R *.dfm}

procedure TFrmFim_Vendas_Beer.btnExecutaClick(Sender: TObject);
begin
// 200, 325
  try
    btnExecuta.Enabled := False;
    btnRetorna.Enabled := False;

    QRel.Sql.Clear;

    if Tipo_Rel.Text = 'Analítico' then
    begin
      if (Dtmen.Date = FrmPrincipal.Abertura.FieldByName('DT_MOVIMENTO').AsDateTime) and (Dtmai.Date = FrmPrincipal.Abertura.FieldByName('DT_MOVIMENTO').AsDateTime) then
      begin
        if Preco.Text = 'Custo de Compra' then
          QRel.Sql.Add('SELECT BAR.COD_PRODUTO PRODUTO_ID, PRODUTOS.DESCRICAO, PRODUTOS.COD_BARRA, TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                       'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.CUSTO_COMPRA) CUSTO, 0 DESCONTO')
        else if Preco.Text = 'Valor de Compra' then
          QRel.Sql.Add('SELECT BAR.COD_PRODUTO PRODUTO_ID, PRODUTOS.DESCRICAO, PRODUTOS.COD_BARRA, TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                       'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.VALOR_COMPRA) CUSTO, 0 DESCONTO')
        else if Preco.Text = 'Customédio' then
          QRel.Sql.Add('SELECT BAR.COD_PRODUTO PRODUTO_ID, PRODUTOS.DESCRICAO, PRODUTOS.COD_BARRA, TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                       'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.CUSTOMEDIO) CUSTO, 0 DESCONTO')
        else
          QRel.Sql.Add('SELECT BAR.COD_PRODUTO PRODUTO_ID, PRODUTOS.DESCRICAO, PRODUTOS.COD_BARRA, TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                       'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.CUSTOMEDIO) CUSTO, 0 DESCONTO');

        QRel.Sql.Add('FROM BAR');
        QRel.Sql.Add('INNER JOIN TRANSACOES');
        QRel.Sql.Add('ON (BAR.TRANSACAO_ID = TRANSACOES.TRANSACAO_ID)');
        QRel.Sql.Add('INNER JOIN PRODUTOS');
        QRel.Sql.Add('ON (BAR.COD_PRODUTO = PRODUTOS.PRODUTO_ID)');
        QRel.Sql.Add('INNER JOIN TABELAS');
        QRel.Sql.Add('ON (PRODUTOS.GRUPO_ID = TABELAS.TABELA_ID AND TABELAS.TIPO_TABELA = :TIPO_TABELA)');
        QRel.Sql.Add('WHERE');
        QRel.Sql.Add('(BAR.DATA BETWEEN :DT_INICIAL AND :DT_FINAL)');
        QRel.Sql.Add('AND (TRANSACOES.CONDUTA = :CONDUTA)');
        QRel.Sql.Add('AND (TRANSACOES.DEPTO = :DEPTO)');
        //QRel.Sql.Add('AND (TRANSACOES.TPCTB = :TPCTB)');
        QRel.Sql.Add('AND (TRANSACOES.EMPRESA_ID = :EMPRESA_ID)');
        QRel.Sql.Add('AND (TRANSACOES.BALANCO <> :BALANCO)');
        QRel.Sql.Add('AND (BAR.CANCELADO = :CANCELADO)');
        if not DeliveryBalcao.Checked then
          QRel.Sql.Add('AND (BAR.MESA < 600 )');
        QRel.Sql.Add('AND (TRANSACOES.TRANSACAO_ID NOT IN(SELECT TRANSACAO_ID FROM NOTAS_CANCELADAS UNION ALL SELECT TRANSACAO_ID FROM NOTAS_CANCELADAS_NFCE))');

        if Tipo_Venda.Text <> '' then
        begin
          QRel.Sql.Add('AND (TRANSACOES.TIPO_VENDA = :TIPO_VENDA)');
          QRel.ParamByName('TIPO_VENDA').AsString  := Tipo_Venda.Text;
        end;
      end
      else
      begin
        if Preco.Text = 'Custo de Compra' then
          QRel.Sql.Add('SELECT BAR.COD_PRODUTO PRODUTO_ID, PRODUTOS.DESCRICAO, PRODUTOS.COD_BARRA, TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                       'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.CUSTO_COMPRA) CUSTO, 0 DESCONTO')
        else if Preco.Text = 'Valor de Compra' then
          QRel.Sql.Add('SELECT BAR.COD_PRODUTO PRODUTO_ID, PRODUTOS.DESCRICAO, PRODUTOS.COD_BARRA, TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                       'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.VALOR_COMPRA) CUSTO, 0 DESCONTO')
        else if Preco.Text = 'Customédio' then
          QRel.Sql.Add('SELECT BAR.COD_PRODUTO PRODUTO_ID, PRODUTOS.DESCRICAO, PRODUTOS.COD_BARRA, TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                       'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.CUSTOMEDIO) CUSTO, 0 DESCONTO')
        else
         QRel.Sql.Add('SELECT BAR.COD_PRODUTO PRODUTO_ID, PRODUTOS.DESCRICAO, PRODUTOS.COD_BARRA, TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                       'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.CUSTOMEDIO) CUSTO, 0 DESCONTO');

        QRel.Sql.Add('FROM BAR');
        QRel.Sql.Add('INNER JOIN TRANSACOES');
        QRel.Sql.Add('ON (BAR.TRANSACAO_ID = TRANSACOES.TRANSACAO_ID)');
        QRel.Sql.Add('INNER JOIN PRODUTOS');
        QRel.Sql.Add('ON (BAR.COD_PRODUTO = PRODUTOS.PRODUTO_ID)');
        QRel.Sql.Add('INNER JOIN TABELAS');
        QRel.Sql.Add('ON (PRODUTOS.GRUPO_ID = TABELAS.TABELA_ID AND TABELAS.TIPO_TABELA = :TIPO_TABELA)');
       { ALTERADO POR CLENIO BARROS 10/06/13 MOTIVO SEM VALOR DE CUSTO PARA ALGUNS PRODUTOS Q NAO POSSUIAM MOVIMENTACAO
         DE VENDA POIS NAO E GERADO POSICAO DE ESTOQUE PARA VENDA DE PRODUTOS
       QRel.Sql.Add('LEFT JOIN POSICAO_PRODUTOS');
        QRel.Sql.Add('ON (TRANSITENS.PRODUTO_ID = POSICAO_PRODUTOS.PRODUTO_ID AND POSICAO_PRODUTOS.DT_MOVIMENTO BETWEEN :DT_INICIAL AND :DT_FINAL )');}
        QRel.Sql.Add('WHERE');
        QRel.Sql.Add('(BAR.DATA BETWEEN :DT_INICIAL AND :DT_FINAL)');
        QRel.Sql.Add('AND (TRANSACOES.CONDUTA = :CONDUTA)');
        QRel.Sql.Add('AND (TRANSACOES.DEPTO = :DEPTO)');
        //QRel.Sql.Add('AND (TRANSACOES.TPCTB = :TPCTB)');
        QRel.Sql.Add('AND (TRANSACOES.EMPRESA_ID = :EMPRESA_ID)');
        QRel.Sql.Add('AND (TRANSACOES.BALANCO <> :BALANCO)');
        QRel.Sql.Add('AND (BAR.CANCELADO = :CANCELADO )');
        if not DeliveryBalcao.Checked then
          QRel.Sql.Add('AND (BAR.MESA < 600 )');
        QRel.Sql.Add('AND (TRANSACOES.TRANSACAO_ID NOT IN(SELECT TRANSACAO_ID FROM NOTAS_CANCELADAS UNION ALL SELECT TRANSACAO_ID FROM NOTAS_CANCELADAS_NFCE))');




        if Tipo_Venda.Text <> '' then
        begin
          QRel.Sql.Add('AND (TRANSACOES.TIPO_VENDA = :TIPO_VENDA)');
          QRel.ParamByName('TIPO_VENDA').AsString  := Tipo_Venda.Text;
        end;
      end;

      if StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Fornecedor')) > 0 then
      begin
        QRel.Sql.Add('AND (PRODUTOS.FORNECEDOR_ID = :FORNECEDOR_ID)');

        QRel.ParamByName('FORNECEDOR_ID').AsInteger := StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Fornecedor'));
      end;

      if StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Grupo')) > 0 then
      begin
        QRel.Sql.Add('AND (PRODUTOS.GRUPO_ID = :GRUPO_ID)');

        QRel.ParamByName('GRUPO_ID').AsInteger := StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Grupo'));
      end;

      if StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'SubTipo')) > 0 then
      begin
        QRel.Sql.Add('AND (PRODUTOS.SUBTIPO_ID = :SUBTIPO_ID)');

        QRel.ParamByName('SUBTIPO_ID').AsInteger := StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'SubTipo'));
      end;

      if StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Localização')) > 0 then
      begin
        QRel.Sql.Add('AND (PRODUTOS.LOCALIZACAO_ID = :LOCALIZACAO_ID)');

        QRel.ParamByName('LOCALIZACAO_ID').AsInteger := StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Localização'));
      end;

      if StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Produto Inicial')) > 0 then
      begin
        QRel.Sql.Add('AND (PRODUTOS.PRODUTO_ID BETWEEN :PRODUTO_INICIAL AND :PRODUTO_FINAL)');

        QRel.ParamByName('PRODUTO_INICIAL').AsInteger := StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Produto Inicial'));
        QRel.ParamByName('PRODUTO_FINAL').AsInteger   := StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Produto Final'));
      end;

      QRel.Sql.Add('GROUP BY BAR.COD_PRODUTO, PRODUTOS.DESCRICAO, PRODUTOS.COD_BARRA, TABELAS.DESCRICAO');

      if Ordem.Text = 'CÓDIGO' then
        QRel.Sql.Add('ORDER BY 4, 1')
      else if Ordem.Text = 'DESCRIÇÃO' then
        QRel.Sql.Add('ORDER BY 4, 2')
      else if Ordem.Text = 'QTD. VENDA' then
        QRel.Sql.Add('ORDER BY 4, 5 DESC')
      else if Ordem.Text = 'VALOR VENDA' then
        QRel.Sql.Add('ORDER BY 4, 6 DESC')
      else
        QRel.Sql.Add('ORDER BY 4, 2');

      QRel.ParamByName('TIPO_TABELA').AsString  := '7';
      QRel.ParamByName('DT_INICIAL').AsDateTime := Dtmen.Date;
      QRel.ParamByName('DT_FINAL').AsDateTime   := Dtmai.Date;
      QRel.ParamByName('CONDUTA').AsString      := '01';
      QRel.ParamByName('DEPTO').AsString        := '07';
      //QRel.ParamByName('TPCTB').AsString        := FrmData.QAcesso.FieldByName('TPCTB').AsString;
      QRel.ParamByName('EMPRESA_ID').AsInteger  := FrmData.QAcesso.FieldByName('EMPRESA_ID').AsInteger;
      QRel.ParamByName('BALANCO').AsString      := 'AGRUPADO';
      QRel.ParamByName('CANCELADO').AsString    := '0';

      QRLabel13.Caption := 'Período: ' + Dtmen.Text + ' a ' + Dtmai.Text;
    end
    else
    begin
      if (Dtmen.Date = FrmPrincipal.Abertura.FieldByName('DT_MOVIMENTO').AsDateTime) and (Dtmai.Date = FrmPrincipal.Abertura.FieldByName('DT_MOVIMENTO').AsDateTime) then
      begin
        if Preco.Text = 'Custo de Compra' then
          QRel.Sql.Add('SELECT TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                       'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.CUSTO_COMPRA) CUSTO, 0 DESCONTO')
        else if Preco.Text = 'Valor de Compra' then
          QRel.Sql.Add('SELECT TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                       'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.VALOR_COMPRA) CUSTO, 0 DESCONTO')
        else if Preco.Text = 'Customédio' then
          QRel.Sql.Add('SELECT TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                        'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.CUSTOMEDIO) CUSTO, 0 DESCONTO')
        else
          QRel.Sql.Add('SELECT TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                        'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.CUSTOMEDIO) CUSTO, 0 DESCONTO');

        QRel.Sql.Add('FROM BAR');
        QRel.Sql.Add('INNER JOIN TRANSACOES');
        QRel.Sql.Add('ON (BAR.TRANSACAO_ID = TRANSACOES.TRANSACAO_ID)');
        QRel.Sql.Add('INNER JOIN PRODUTOS');
        QRel.Sql.Add('ON (BAR.COD_PRODUTO = PRODUTOS.PRODUTO_ID)');
        QRel.Sql.Add('INNER JOIN TABELAS');
        QRel.Sql.Add('ON (PRODUTOS.GRUPO_ID = TABELAS.TABELA_ID AND TABELAS.TIPO_TABELA = :TIPO_TABELA)');
        QRel.Sql.Add('WHERE');
        QRel.Sql.Add('(BAR.DATA BETWEEN :DT_INICIAL AND :DT_FINAL)');
        QRel.Sql.Add('AND (TRANSACOES.CONDUTA = :CONDUTA)');
        QRel.Sql.Add('AND (TRANSACOES.DEPTO = :DEPTO)');
        //QRel.Sql.Add('AND (TRANSACOES.TPCTB   = :TPCTB)');
        QRel.Sql.Add('AND (TRANSACOES.EMPRESA_ID = :EMPRESA_ID)');
        QRel.Sql.Add('AND (TRANSACOES.BALANCO <> :BALANCO)');
        QRel.Sql.Add('AND (BAR.CANCELADO = :CANCELADO)');
        if not DeliveryBalcao.Checked then
          QRel.Sql.Add('AND (BAR.MESA < 600 )');
        QRel.Sql.Add('AND (TRANSACOES.TRANSACAO_ID NOT IN(SELECT TRANSACAO_ID FROM NOTAS_CANCELADAS UNION ALL SELECT TRANSACAO_ID FROM NOTAS_CANCELADAS_NFCE))');

        if Tipo_Venda.Text <> '' then
        begin
          QRel.Sql.Add('AND (TRANSACOES.TIPO_VENDA = :TIPO_VENDA)');
          QRel.ParamByName('TIPO_VENDA').AsString  := Tipo_Venda.Text;
        end;
      end
      else
      begin
       if Preco.Text = 'Custo de Compra' then
          QRel.Sql.Add('SELECT TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                       'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.CUSTO_COMPRA) CUSTO, 0 DESCONTO')
        else if Preco.Text = 'Valor de Compra' then
          QRel.Sql.Add('SELECT TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                       'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.VALOR_COMPRA) CUSTO, 0 DESCONTO')
        else if Preco.Text = 'Customédio' then
          QRel.Sql.Add('SELECT TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                        'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.CUSTOMEDIO) CUSTO, 0 DESCONTO')
        else
          QRel.Sql.Add('SELECT TABELAS.DESCRICAO GRUPO, SUM(BAR.QUANTIDADE) QTD_VENDA, ' +
                        'SUM(BAR.VR_TOTAL) VR_VENDA, SUM(BAR.QUANTIDADE * PRODUTOS.CUSTOMEDIO) CUSTO, 0 DESCONTO');

        QRel.Sql.Add('FROM BAR');
        QRel.Sql.Add('INNER JOIN TRANSACOES');
        QRel.Sql.Add('ON (BAR.TRANSACAO_ID = TRANSACOES.TRANSACAO_ID)');
        QRel.Sql.Add('INNER JOIN PRODUTOS');
        QRel.Sql.Add('ON (BAR.COD_PRODUTO = PRODUTOS.PRODUTO_ID)');
        QRel.Sql.Add('INNER JOIN TABELAS');
        QRel.Sql.Add('ON (PRODUTOS.GRUPO_ID = TABELAS.TABELA_ID AND TABELAS.TIPO_TABELA = :TIPO_TABELA)');
        QRel.Sql.Add('LEFT JOIN POSICAO_PRODUTOS');
        QRel.Sql.Add('ON (BAR.COD_PRODUTO = POSICAO_PRODUTOS.PRODUTO_ID AND BAR.DATA = POSICAO_PRODUTOS.DT_MOVIMENTO)');
        QRel.Sql.Add('WHERE');
        QRel.Sql.Add('(BAR.DATA BETWEEN :DT_INICIAL AND :DT_FINAL)');
        QRel.Sql.Add('AND (TRANSACOES.CONDUTA = :CONDUTA)');
        QRel.Sql.Add('AND (TRANSACOES.DEPTO = :DEPTO)');
        //QRel.Sql.Add('AND (TRANSACOES.TPCTB   = :TPCTB)');
        QRel.Sql.Add('AND (TRANSACOES.EMPRESA_ID = :EMPRESA_ID)');
        QRel.Sql.Add('AND (TRANSACOES.BALANCO <> :BALANCO)');
        QRel.Sql.Add('AND (BAR.CANCELADO = :CANCELADO )');
        if not DeliveryBalcao.Checked then
          QRel.Sql.Add('AND (BAR.MESA < 600 )');
        QRel.Sql.Add('AND (TRANSACOES.TRANSACAO_ID NOT IN(SELECT TRANSACAO_ID FROM NOTAS_CANCELADAS UNION ALL SELECT TRANSACAO_ID FROM NOTAS_CANCELADAS_NFCE))');

        if Tipo_Venda.Text <> '' then
        begin
          QRel.Sql.Add('AND (TRANSACOES.TIPO_VENDA = :TIPO_VENDA)');
          QRel.ParamByName('TIPO_VENDA').AsString  := Tipo_Venda.Text;
        end;
      end;

      if StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Fornecedor')) > 0 then
      begin
        QRel.Sql.Add('AND (PRODUTOS.FORNECEDOR_ID = :FORNECEDOR_ID)');

        QRel.ParamByName('FORNECEDOR_ID').AsInteger := StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Fornecedor'));
      end;

      if StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Grupo')) > 0 then
      begin
        QRel.Sql.Add('AND (PRODUTOS.GRUPO_ID = :GRUPO_ID)');

        QRel.ParamByName('GRUPO_ID').AsInteger := StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Grupo'));
      end;

      if StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'SubTipo')) > 0 then
      begin
        QRel.Sql.Add('AND (PRODUTOS.SUBTIPO_ID = :SUBTIPO_ID)');

        QRel.ParamByName('SUBTIPO_ID').AsInteger := StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'SubTipo'));
      end;

      if StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Localização')) > 0 then
      begin
        QRel.Sql.Add('AND (PRODUTOS.LOCALIZACAO_ID = :LOCALIZACAO_ID)');

        QRel.ParamByName('LOCALIZACAO_ID').AsInteger := StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Localização'));
      end;

      if StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Produto Inicial')) > 0 then
      begin
        QRel.Sql.Add('AND (PRODUTOS.PRODUTO_ID BETWEEN :PRODUTO_INICIAL AND :PRODUTO_FINAL)');

        QRel.ParamByName('PRODUTO_INICIAL').AsInteger := StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Produto Inicial'));
        QRel.ParamByName('PRODUTO_FINAL').AsInteger   := StrToInt(LeIni(Arq_Ini, 'Parâmetros Estoque', 'Produto Final'));
      end;

      QRel.Sql.Add('GROUP BY TABELAS.DESCRICAO');

      QRel.Sql.Add('ORDER BY 1');

      QRel.ParamByName('TIPO_TABELA').AsString  := '7';
      QRel.ParamByName('DT_INICIAL').AsDateTime := Dtmen.Date;
      QRel.ParamByName('DT_FINAL').AsDateTime   := Dtmai.Date;
      QRel.ParamByName('CONDUTA').AsString      := '01';
      QRel.ParamByName('DEPTO').AsString        := '07';
      //QRel.ParamByName('TPCTB').AsString        := FrmData.QAcesso.FieldByName('TPCTB').AsString;
      QRel.ParamByName('EMPRESA_ID').AsInteger  := FrmData.QAcesso.FieldByName('EMPRESA_ID').AsInteger;
      QRel.ParamByName('BALANCO').AsString      := 'AGRUPADO';
      QRel.ParamByName('CANCELADO').AsString    := '0';

      QRLabel16.Caption := 'Período: ' + Dtmen.Text + ' a ' + Dtmai.Text;
    end;

    QRel.Prepare;
    QRel.Open;

    if QRel.IsEmpty then
      Application.MessageBox('Não há dados para os parâmetros informados', PChar(Msg_Title), mb_IconInformation)
    else
    begin
      if Tipo_Rel.Text = 'Analítico' then
      begin
        ExportarExcel.PreviewModal; //Fim.PreviewModal
        ExportarExcel.ExportToFilter(TQRXLSFilter.Create(ExtractFilePath(ParamStr(0)) + 'Exportados\Fim_Vendas.xls'));
        Application.MessageBox(PChar('Arquivo exportado para : ' + ExtractFilePath(ParamStr(0)) + 'Exportados\Fim_Vendas.xls') , PChar(Msg_Title), mb_IconInformation);
      end
      else
        Sintetico.PreviewModal;

    end;
  finally
    btnExecuta.Enabled := True;
    btnRetorna.Enabled := True;
  end;
end;

procedure TFrmFim_Vendas_Beer.btnRetornaClick(Sender: TObject);
begin
  Close;
end;

procedure TFrmFim_Vendas_Beer.DtmenEnter(Sender: TObject);
begin
  Keybd_Event(VK_LEFT, 0, 0, 0);
end;

procedure TFrmFim_Vendas_Beer.DtmenKeyDown(Sender: TObject; var Key: Word;
  Shift: TShiftState);
begin
  if (Key = Vk_Return) or (Key = Vk_Down) then
   // Perform(Wm_NextDlgctl, 0, 0);

  if Key = Vk_Up then
    Perform(Wm_NextDlgctl, 1, 0);
end;

procedure TFrmFim_Vendas_Beer.FormClose(Sender: TObject; var Action: TCloseAction);
begin
  Action := caFree;
end;

procedure TFrmFim_Vendas_Beer.FormCreate(Sender: TObject);
begin
  Dtmen.Date := StrToDate('01/' + Copy(DateToStr(date), 4, 7));
  Dtmai.Date := Ult_Dia_Mes(date);
end;

procedure TFrmFim_Vendas_Beer.OrdemKeyDown(Sender: TObject; var Key: Word;
  Shift: TShiftState);
begin
  if Key = Vk_Return then
    Perform(Wm_NextDlgctl, 0, 0);
end;

end.
